import { TextField } from '../text-field';
import { TextAria } from '../text-aria';
import { Password } from '../password';

export const Fields = {
  text: TextField,
  textaria: TextAria,
  password: Password,
};
